RUNNING THE SIMULATION

To start the simulation, enter the following in the command line:

>> java -jar ../logist/logist.jar config/reactive.xml reactive-random

To run the simulation with two agents, enter:

>> java -jar ../logist/logist.jar config/reactive.xml reactive-random reactive-random

